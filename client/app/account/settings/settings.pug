.container
  .row
    .col-sm-12
      h1 Change Password
    .col-sm-12
      form.form(name='changePasswordForm', (ngSubmit)='changePassword(changePasswordForm)', novalidate='', #changePasswordForm='ngForm')
        .form-group
          label Current Password
          input.form-control(type='password', name='password', [(ngModel)]='user.oldPassword', required='', #oldPassword='ngModel')
          p.help-block([hidden]='oldPassword.valid || !submitted')
            | Required
        .form-group
          label New Password
          input.form-control(type='password', name='newPassword', [(ngModel)]='user.newPassword', required='', #newPassword='ngModel', minlength='3')
          p.help-block([hidden]='newPassword.valid || !submitted')
            | Password must be at least 3 characters.
        .form-group
          label Confirm New Password
          input.form-control(type='password', name='confirmPassword', [(ngModel)]='user.confirmPassword', required='', #confirmPassword='ngModel', match='user.newPassword', minlength='3')
          p.help-block([hidden]='confirmPassword.valid || !submitted')
            | Passwords must match.
        p.help-block  {{ message }} 
        button.btn.btn-lg.btn-primary(type='submit') Save changes
